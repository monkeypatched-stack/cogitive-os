"""Dashboard — backend-driven cognitive observation models.

All dashboard data is computed by the backend. The frontend (REPL, CLI, Web UI)
simply renders what the backend returns. No business logic in the presentation layer.
"""
