"""Security capabilities — external integration drivers for ZTAA.

Owned by cerebellum per INV-003: all external integrations are capabilities.

  opa_client    — OPA policy engine HTTP driver
  nats_audit    — NATS audit event publisher
  spire_client  — SPIFFE/SPIRE workload identity driver
  mtls_parser   — TLS client cert extraction and validation
"""
