# Plan: Fix Remaining Validation Defects

## Verified Already Correct (no action needed)
- ConstraintSolver — correct (passes 3-var all_different, sum constraints)
- SAT unit propagation — correct (recursive DPLL handles chains and UNSAT detection)
- MCTS selection — correct (UCB1 picks best child when all visited)
- LLMSolver — fixed (calls ModelBackend with fallback)
- CORS, secrets, CognitiveLoop, ThreadPool, Redis JSON, InfluxDB warning, error handling — all fixed

## Remaining Defects (7 items)

### 1. Auth Middleware on Core Routes (HIGH)
**Files:** `src/monkey_brain/api/main.py`, `routes/run.py`, `routes/prompt.py`, `routes/simulate.py`
Add `get_current_user` dependency. Apply to `/query`, `/prompt`, `/simulate`.

### 2. 8-Stage Capability Lifecycle (MEDIUM)
**File:** `src/monkey_brain/kernel/capabilities/icapability.py`
Add default-implemented lifecycle methods on ICapability ABC.

### 3. JEPAWorldModel Upgrade (LOW)
**File:** `src/monkey_brain/kernel/solver_mesh.py`
Replace linear extrapolation with least-squares regression.

### 4. DDD Aggregates (MEDIUM)
**Files:** `domains/*/aggregates/__init__.py`
Implement aggregate roots for 11 entities.

### 5-7. Missing Tests (MEDIUM)
**Files:** `tests/unit/test_repair.py`, `test_scheduler.py`, `test_persistence.py`

## Verification
- `python3 tests/harden_test.py` — 61/61
- Compile check on all modified files
